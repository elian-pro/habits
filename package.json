{
  "name": "habitflow-ai",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "start": "node build.mjs && npx http-server dist -p 3000 -c-1",
    "dev": "node build.mjs && npx http-server dist -p 3000 -c-1"
  }
}
